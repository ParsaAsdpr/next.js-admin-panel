import React from 'react';

const Income = ({ className }) => {
    return (
        <div className={`${className} bg-white rounded-lg p-10 flex flex-col items-center`}>
        </div>
    );
};

export default Income;